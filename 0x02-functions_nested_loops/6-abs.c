#include <stdio.h>
#include <sdlib.h>
/**
 * _islower- function that computes the absolute value of an integer.
 *
 * @c:the num to check
 *
 * Return: number
 */
int _abs(int c)
{

	return (abs(c));
}

