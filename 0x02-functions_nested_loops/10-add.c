#include <stdio.h>
#include "main.h"

/**
 * add-function that adds two integers and returns the result.
 *@x :the first number
 *@y :the second numer
 * Return: the result
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
